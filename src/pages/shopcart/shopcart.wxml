<import src="/public/template/list/list" />


<view class="cart">
  <text>这是购物车页面</text>
<!-- 
  <view class="page-body">
  <view class="page-body-wrapper">
    <camera device-position="back" flash="off" binderror="error" style="width: 100%; height: 300px;"></camera>
    <view class="btn-area">
      <button type="primary" bindtap="takePhoto">拍照</button>
    </view>
    <view class="btn-area">
      <button type="primary" bindtap="startRecord">开始录像</button>
    </view>
    <view class="btn-area">
      <button type="primary" bindtap="stopRecord">结束录像</button>
    </view>
    <view class="preview-tips">预览</view>
    <image wx:if="{{src}}" mode="widthFix" src="{{src}}"></image>
    <video wx:if="{{videoSrc}}" class="video" src="{{videoSrc}}"></video>
  </view>
</view> -->

  
 <!-- <view>
    <button bindtap="getData">获取后台数据</button>
  </view> -->

<!-- 
  <view wx:if="{{ loading }}">正在加载中。。。。</view> -->
<!-- 
  <view class="movie-list">
    <view class="movie-item" wx:for="{{ movieArr }}">
       <text>电影名称：{{ item.title }}</text>
    </view>
  
  </view>


  <view>
    <button bindtap="getJSON">获取线上json文件接口</button>
  </view> -->

<!-- 
  <button bindtap="getMockData">获取mock数据</button>
  <view class="list">
    <view class="list-item" wx:for="{{ resultArr }}">
      <text>{{ item.address }}</text>
    </view>
  </view> -->



  <!-- 使用模板 -->

<block wx:for="{{ listData  }}">

 <template is="list_tpl" data="{{ ...item }}" />

</block>

</view>

